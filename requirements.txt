# 后端 API（不含 mmpose / SAM）
# 用法: conda activate gxzh && pip install -r requirements.txt

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
sqlalchemy>=2.0,<3
python-dotenv>=1.0,<2
python-multipart>=0.0.9
dashscope>=1.20
openai>=1.0
